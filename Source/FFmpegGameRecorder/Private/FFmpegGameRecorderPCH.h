# pragma once

#include "CoreMinimal.h"
// #include "Resources/Version.h"

DEFINE_LOG_CATEGORY_STATIC(LogRecorder, Log, Log);
// DEFINE_LOG_CATEGORY(LogRecorder);

// #if ENGINE_MAJOR_VERSION >= 5
// using FTickerLocal = FTSTicker;
// using FDelegateHandleLocal = FTSTicker::FDelegateHandle;
// #else
// using FTickerLocal = FTicker;
// using FDelegateHandleLocal = FDelegateHandle;
// #endif